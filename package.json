{
  "name": "mikrotik-hotspot-management-system",
  "version": "1.0.0",
  "description": "Production-ready MikroTik HotSpot Management System (GitHub-First + VPN-Based Architecture)",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:backend": "npm --workspace=apps/backend run dev",
    "dev:frontend": "npm --workspace=apps/frontend run dev",
    "dev:connector": "npm --workspace=apps/connector run dev",
    "dev": "npm --workspace=apps/backend run dev",
    "build:shared": "tsc -p packages/shared/tsconfig.json",
    "build:backend": "npm run build:shared && tsc -p apps/backend/tsconfig.json",
    "build:connector": "npm run build:shared && tsc -p apps/connector/tsconfig.json",
    "build:frontend": "npm run build:shared && npm --workspace=@hotspot/frontend run build",
    "build": "npm run build:backend && npm run build:connector && npm run build:frontend",
    "start": "node apps/backend/dist/server.js",
    "start:backend": "node apps/backend/dist/server.js",
    "start:connector": "node apps/connector/dist/index.js",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "tsc -p packages/shared/tsconfig.json && tsc -p apps/backend/tsconfig.json && tsc -p apps/connector/tsconfig.json",
    "lint": "npm run typecheck"
  },
  "keywords": [
    "mikrotik",
    "hotspot",
    "routeros",
    "vpn",
    "sstp",
    "react",
    "fastify",
    "prisma",
    "bangladesh",
    "wifi-billing"
  ],
  "author": "Antigravity Engineering",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.13.5",
    "typescript": "^5.7.3"
  }
}
